apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nyan-deploy.fullname" . }}-test-connection"
  labels:
    {{- include "nyan-deploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nyan-deploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
