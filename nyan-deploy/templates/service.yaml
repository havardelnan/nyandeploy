apiVersion: v1
kind: Service
metadata:
  name: {{ include "nyan-deploy.fullname" . }}
  labels:
    {{- include "nyan-deploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nyan-deploy.selectorLabels" . | nindent 4 }}
